export enum Gender {
  MALE = "MALE",
  FEMALE = "FEMALE",
  TRANSSEXUAL = "TRANSSEXUAL",
  OTHER = "OTHER",
}

export enum ServiceName {
  DEFAULT = "default",
  GENERAL = "general",
  USER = "user",
}

export enum BusinessSize {
  SMALL = "small",
  Medium = "medium",
  BIG = "big",
}

export enum TokenType {
  TOKEN,
  REFRESH,
}
